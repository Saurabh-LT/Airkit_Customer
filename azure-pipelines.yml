trigger:
      branches:
        include:
          - main

pool:
      vmImage: ubuntu-latest

parameters:
      - name: suiteXmlFile
        displayName: TestNG suite to run
        type: string
        default: LT_TestNG.xml
        values:
          - LT_TestNG.xml
          - TestNG.xml

variables:
      MAVEN_CACHE_FOLDER: $(Pipeline.Workspace)/.m2/repository
      MAVEN_OPTS: -Dmaven.repo.local=$(MAVEN_CACHE_FOLDER)

steps:
      - task: JavaToolInstaller@0
        displayName: Set Java Version
        inputs:
          versionSpec: "8"
          jdkArchitectureOption: x64
          jdkSourceOption: PreInstalled

      - task: Cache@2
        displayName: Cache Maven dependencies
        inputs:
          key: 'maven | "$(Agent.OS)" | pom.xml'
          restoreKeys: |
            maven | "$(Agent.OS)"
            maven
          path: $(MAVEN_CACHE_FOLDER)

      - task: configuration@1
        inputs:
            connection: 'Saurabh-Prod-Cred'
            isTunnelActivate: false
            isAppAutomate: false
            isSmartUI: true

      - script: |
          echo "===== DEBUG CREDS (safe — only lengths + first chars) ====="
          echo "LT_USERNAME    length=${#LT_USERNAME}   first3='${LT_USERNAME:0:3}'   last2='${LT_USERNAME: -2}'"
          echo "LT_ACCESS_KEY  length=${#LT_ACCESS_KEY} first5='${LT_ACCESS_KEY:0:5}' last3='${LT_ACCESS_KEY: -3}'"
          echo "Expected:      LT_USERNAME length=17 first3='sau' last2='st'"
          echo "Expected:      LT_ACCESS_KEY length=50 first5='LT_oZ' last3='GBc'"
          echo "==========================================================="
          mvn -B -e test \
            -DsuiteXmlFile=${{ parameters.suiteXmlFile }} \
            -DLT_USER_NAME="$LT_USERNAME" \
            -DLT_ACCESS_KEY="$LT_ACCESS_KEY" \
            -DBUILD_NAME="Azure-$(Build.BuildNumber)" \
            -DSMARTUI_PROJECT="running from Azure"
        displayName: Run TestNG Suite
        env:
          LT_USERNAME: $(LT_USERNAME)
          LT_ACCESS_KEY: $(LT_ACCESS_KEY)

      - task: PublishTestResults@2
        displayName: Publish TestNG Results
        condition: succeededOrFailed()
        inputs:
          testResultsFormat: JUnit
          testResultsFiles: "**/surefire-reports/TEST-*.xml"
          testRunTitle: LambdaTest Results

      - task: PublishPipelineArtifact@1
        displayName: Publish Surefire Reports
        condition: succeededOrFailed()
        inputs:
          targetPath: target/surefire-reports
          artifact: test-reports
          publishLocation: pipeline

      - task: LambdatestResults@1
